import 'package:flutter/material.dart';

class VoirSeance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Voir Séance "),
      ),
    );
  }
}
